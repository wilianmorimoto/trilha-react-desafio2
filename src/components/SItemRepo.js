import styled from "styled-components"

export const SItemContainer = styled.div`
	width: 80%;

	h3 {
		font-size: 32px;
		color: #fafafa;
	}

	p {
		font-size: 16px;
		color: #fafafa;
		opacity: 0.6;
	}

  a {
    margin: 20px 0;
    display: block;
		width: fit-content;
		color: inherit;
  }

	.remover {
		color: #ff0000;
	}

	hr {
		margin: 20px 0;
	}
`
